# 模型来源与授权政策

VoxWeave 官方源码仓库不包含 RVC 声音模型。官方目录只接受同时满足以下条件的条目：来源是可追溯的 HTTPS 页面；模型作者明确允许下载和再分发；条目提供 SPDX 许可证、最终文件大小和 SHA-256；角色、真人声音或训练数据的使用没有已知授权冲突。

许可证不明、只允许个人使用、禁止再分发或来源无法追溯的模型不能进入官方目录。用户仍可在自己有权使用的前提下从本地路径导入；这类导入会保留 `LicenseRef-Unknown` 或用户声明的许可证，仅存在于本机数据库中，不会自动上传或提交到目录。

`.pth` 在独立 RVC 进程中用 `torch.load(..., weights_only=True)` 检查。VoxWeave 自动计算最终 SHA-256，并按名称查找 `.index`。只有一个有效候选时自动配对；多个候选会标记 `index_choice_required`，等待用户显式指定，不按文件时间或目录顺序猜测。

同一稳定 ID 发现不同权重哈希时导入失败，不覆盖原记录。训练检查点按 family 与 epoch 登记，界面可以一起展示，但不会把多个 epoch 冒充为彼此无关的正式声音。预设记录模型哈希；模型文件变化后预设返回 `needs_reconfirmation: true`。

使用声音模型时，用户负责取得声音主体、模型作者和素材权利人的必要许可，并遵守适用法律与平台政策。VoxWeave 的本地处理和开源许可不替用户授予这些权利。
