[build-system]
requires = ["setuptools==80.9.0", "wheel==0.45.1"]
build-backend = "setuptools.build_meta"

[project]
name = "voxweave"
version = "0.2.0"
description = "Local-first high-quality offline and realtime RVC voice conversion workstation"
readme = "README.md"
requires-python = ">=3.12,<3.13"
license = {text = "AGPL-3.0-or-later"}
authors = [{name = "CheshireMew"}]
dependencies = [
  "fastapi==0.116.1",
  "uvicorn==0.35.0",
  "websockets==15.0.1",
  "pydantic==2.11.7",
  "PySide6==6.11.1",
  "numpy==1.26.4",
  "soundfile==0.14.0",
  "scipy==1.17.1",
]

[project.optional-dependencies]
dev = [
  "httpx==0.28.1",
  "jsonschema==4.25.0",
  "pytest==8.4.1",
  "pytest-cov==6.2.1",
  "ruff==0.12.7",
]

[project.scripts]
voxweave = "voxweave.cli:main"
voxweave-service = "voxweave.service:main"
voxweave-gui = "voxweave.gui:main"
voxweave-app = "voxweave.app:main"
voxweave-mcp = "voxweave.mcp_server:main"

[tool.setuptools]
package-dir = {"" = "src"}
include-package-data = true

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
voxweave = ["qml/*.qml", "resources/*.json"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
